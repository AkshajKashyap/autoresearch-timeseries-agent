experiment:
  name: persistence

dataset:
  name: synthetic
  n_timesteps: 600
  n_features: 4
  input_length: 48
  forecast_horizon: 24
  train_fraction: 0.7
  val_fraction: 0.15
  seed: 42

model:
  name: persistence

reporting:
  runs_dir: reports/runs
