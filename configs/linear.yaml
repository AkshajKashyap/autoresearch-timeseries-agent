experiment:
  name: linear

dataset:
  name: synthetic
  mode: nonlinear
  split_strategy: chronological
  n_timesteps: 600
  n_features: 4
  input_length: 48
  forecast_horizon: 24
  train_fraction: 0.7
  val_fraction: 0.15
  seed: 42

model:
  name: linear
  alpha: 1.0

training:
  scale_features: false
  normalize_target: false

reporting:
  runs_dir: reports/runs
